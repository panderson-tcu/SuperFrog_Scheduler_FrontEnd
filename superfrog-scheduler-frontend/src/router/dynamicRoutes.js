//not all users can see the views defined here, like a user must have 'orders' permission to see /orders
// see loadDynamicRoutes.js for how to filter views based on user permissions

const dynamicRoutes = [
    
]

export default dynamicRoutes;